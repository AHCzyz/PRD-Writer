- 所有 UI 组件均通过 `useEditorStore` 直接订阅全局状态切片，避免 Props Drilling。
- 核心数据模型（TabMLDocument）的变更操作（如 updateCell, insertRow）在 Store 中统一处理并自动触发 `serialize` 同步 sourceText。
- 自定义格式解析与序列化逻辑严格隔离在 `src/core/tabml/` 目录下，保持纯函数实现以便测试。